{% if has_fb_feed() %}
    {% include 'AppBundle:fragment:feed-list-photo.html.twig' with {
    'feed'  : get_fb_feed(),
    'count' : config('feed.count.photos', 12)
    } only %}
{% else %}
    {% include 'AppBundle:fragment:action-status-icon.html.twig' with {
    'icon'       : 'fa-times',
    'text'       : 'Could not load photos feed...'
    } only %}
{% endif %}
