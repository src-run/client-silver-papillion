<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://fonts.googleapis.com/css?family=Roboto+Condensed:100,300,400,700%7CRoboto+Slab%7CRoboto:100,300,400,500" rel="stylesheet">
    <title>Silver Papillon: {{ title }}</title>
    {% block stylesheets %}
        <style type="text/css">
            body {
                margin      : 0;
                padding     : 0;
                min-width   : 100% !important;
                font-family : Roboto, sans-serif;
                background  : #f2f1f3;
            }
            hr {
                margin        : 72px 0 60px 0;
                border        : none;
                border-bottom : 1px solid #74a4ca;
            }
            #header {
                padding    : 36px 0;
                width      : 100%;
                background : #2196f3;
                box-shadow : 0 0 40px rgba(0, 0, 0, .5);
            }
            #header > * {
                padding : 0 42px;
            }
            #header h1 {
                color         : #fff;
                font-weight   : 200;
                font-family   : "Roboto Condensed", Roboto, sans-serif;
                margin-bottom : 6px;
            }
            #header .header-logo {
                float : right;
            }
            #header .header-logo img {
                height : 110px;
            }
            #header .header-info {
                color       : #fff;
                font-weight : 200;
                font-size   : 21px;
            }
            #header .header-info p {
                display        : block;
                padding        : 0;
                margin-bottom  : 21px;
                text-transform : uppercase;
                font-size      : 15px;
                font-weight    : 200;
                font-style     : normal;
            }
            #header .header-info p cite, #header .header-info p time {
                font-style : normal;
            }
            #body {
                margin    : 29px auto;
                padding   : 0 29px;
                width     : 100%;
                max-width : 1180px;
                overflow  : hidden;
            }
            #body p {
                font-size   : 15px;
                font-weight : 300;
            }
            #body h1, #body h2, #body h3, #body h4, #body h5, #body h6 {
                font-weight : 300;
            }
            #body h2 {
                font-size   : 54px;
                font-weight : 200;
                margin      : 54px 0 36px 0;
            }
            #body .lead {
                margin      : 21px 0 36px 0;
                color       : #6b6b6b;
                font-size   : 18px;
                line-height : 36px;
                font-weight : 300;
            }
        </style>
    {% endblock %}
</head>
<body>
<div id="header">
    <div class="header-logo">
        <img src="{{ asset('logo-small.png', 'images') }}" alt="Silver Papillon Logo" />
    </div>
    <div class="header-title">
        <h1>Silver Papillon: {{ title }}</h1>
    </div>
    <div class="header-info">
        <p>Sent on <time>{{ createdOn|date('r') }}</time> from <cite>{{ from }}</cite> server.</p>
    </div>
</div>
<div id="body">
    {% block body %}
    {% endblock %}
</div>
</body>
</html>
